name: Schedule a job
on:
  schedule:
    - cron: '* * * * *'
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Print current time
        run: echo "The current server time: $(date)"
